---
// DISABLED: This page uses Strapi API which has been removed
// TODO: Convert to use local Markdown files from src/content/blog/
// Uncomment and modify when implementing local MD file support

/*
import CallToAction from "@/components/sections/CallToAction.astro";
import Base from "@/layouts/Base.astro";
import LayoutWrapper from "@/components/LayoutWrapper.astro";
import type { Post } from "@/types";

export async function getStaticPaths() {
  // TODO: Replace with local markdown file reading from src/content/blog/
  return [];
}

const { single } = Astro.params as { single: string };

// TODO: Replace with local markdown file reading
const postData = {};
*/
---

<!-- DISABLED: Convert to use local Markdown files -->
<h1>Blog post functionality disabled. TODO: Implement local MD file support.</h1>
<p>This page will be converted to use local Markdown files from src/content/blog/ instead of Strapi CMS.</p>